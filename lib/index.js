// dsh-assistant-optimization — Host module (no-op)
// All logic is client-side. The host half exists only because dsh.bundle
// requires a Cordis plugin entry point.
function apply(ctx) {}
export { apply };
